# TODO

- [ ] ランキング上位のうち `住所解決レベル != gaiku` の銘柄を抽出し、優先調査リストを作る
- [ ] 優先調査リストの先頭から `resolve-address` で番地レベル住所を特定し、`config/address_overrides.yaml` に反映する
- [ ] 倉庫・工場地なのに商業地を拾うケースを減らすため、地価推定ロジックの用途ミスマッチ補正を設計する
- [ ] `全国各所` へ退避した面積をランキング上で追跡できるようにする
- [ ] 既知ケースの回帰テストを追加し、精度改善の効果を固定サンプルで検証できるようにする

## Notes

- 2026-03-19: まずは `gaiku` 未満の高インパクト案件を機械的に洗い出す
